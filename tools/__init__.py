"""Out-of-band tooling that ships with sqlite-memory-mcp.

Submodules under `tools/` extend the core MCP server with adapters and
auxiliary utilities. Each subpackage is independently importable; the
core servers never depend on `tools/*`.
"""
